name: Daily Vercel Redeploy

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  redeploy:
    runs-on: ubuntu-latest
    steps:
      - name: Redeploy
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY }}"
